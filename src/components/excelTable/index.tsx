import React from 'react';
import { ReadTable } from './readTable';

const filename = './combinate.xlsx';
const sheetname = 'list1';

const ExcelTable = () => {
    // ReadTable(filename, sheetname);
    return (
        <div>
            <p>Тут должна быть таблица</p>
        </div>
    )
}

export default ExcelTable;